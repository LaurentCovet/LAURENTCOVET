
  # LAURENT COVET

  This is a code bundle for LAURENT COVET. The original project is available at https://www.figma.com/design/j3W1xfICdbMRAV8it6Xuga/LAURENT-COVET.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  